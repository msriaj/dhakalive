# Payload and Next both rely on hoisted peer resolution for their plugin graph.
# Keeping strict-peer-dependencies on is what surfaces an out-of-range Next/React
# before it becomes a runtime failure inside the admin bundle.
strict-peer-dependencies=true
auto-install-peers=true
resolution-mode=highest
enable-pre-post-scripts=true
